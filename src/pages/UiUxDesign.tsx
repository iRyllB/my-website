import React from "react";

const UiUxDesign: React.FC = () => (
  <div style={{ padding: "2rem", textAlign: "center" }}>
    <h1>UI / UX Design</h1>
    <p>This is a placeholder page for UI / UX Design.</p>
  </div>
);

export default UiUxDesign;
